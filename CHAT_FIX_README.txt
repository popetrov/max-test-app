Чат исправлен.

Теперь:
1. Если проект запущен через обычный serve — чат работает в демо-режиме и сохраняет сообщения в браузере.
2. Если проект запущен через npm start — чат работает через сервер Socket.IO, сообщения видны всем подключенным пользователям.

Для полноценного общего чата:
npm install
npm start
Открыть: http://localhost:3000
